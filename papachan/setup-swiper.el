;;; setup-swiper.el --- Summary
;;; Commentary:
;;; Code:
(require 'swiper)

(use-package swiper
  :bind (("C-s" . swiper)
         ("C-r" . swiper)))

(provide 'setup-swiper)
;;; setup-swiper.el ends here
